# FirebaseBoilerplate

Boilerplate install using rebase

Replace the values in firebaseValues.js with the values from 'Add firebase to your web application", add firebaseValues to your .gitignore so that no one can get your key and you should be good to go.
